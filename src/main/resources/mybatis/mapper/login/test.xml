<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="test.arthall.con_node.testMap">
    <select id="getTest" resultType="test.arthall.con_node.testModel">
        SELECT no,title,writer,ins_date,upd_date,likes,counts FROM board;
    </select>
</mapper>